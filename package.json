{
    "name": "auth-test-frontend",
    "version": "1.0.0",
    "description": "Authentication test frontend",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "serve": "^14.2.1",
        "live-server": "^1.2.2"
    }
}